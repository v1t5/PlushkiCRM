# PHP / Composer
vendor/
composer.lock
*.cache
.phpunit.cache/
.composer-cache/

# Symfony
/services/*/var/

# Local env / secrets (commit .env.example, never .env)
.env
.env.local
.env.*.local

# Secrets / keys
*.pem

# Local dev-only material (kept in the working tree, never published)
/dev/
/CLAUDE.md

# OS / editor
.DS_Store
.idea/
.vscode/
*.log
